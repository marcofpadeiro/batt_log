use ratatui::{buffer::Buffer, layout::Rect};

use crate::app::App;

impl App {
    pub fn render_graph(&self, graph_area: Rect, buf: &mut Buffer) {
        todo!()
    }

    pub fn render_info(&self, info_area: Rect, buf: &mut Buffer) {
        todo!()
    }

    pub fn render_mode(&self, mode_area: Rect, buf: &mut Buffer) {
        todo!()
    }

    pub fn render_list(&self, list_area: Rect, buf: &mut Buffer) {
        todo!()
    }
}
